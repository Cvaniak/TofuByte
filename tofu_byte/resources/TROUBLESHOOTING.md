# Input problem

If you get message about problem with input system,
it is caused by the way this game hanldes input.

PYNPUT_BACKEND_KEYBOARD=uinput uv run textual run --dev tofu_byte.command_line:run --debug contact_dir,step,fps,footer

or

sudo tofu_byte

or

run terminal with sudo
